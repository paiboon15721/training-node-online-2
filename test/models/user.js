const { assert } = require('chai')

describe('User', () => {
  it('Create user', async () => {
    assert.equal(true, true)
  })

  it('Delete user', async () => {
    assert.equal(true, true)
  })
})
